# Windows 11 / CPython 3.12 / CUDA 12.4 local voice/model install profile.
#
# This is intentionally not called a lock yet. The current machine snapshot is
# recorded under requirements/locks, but the combined ASR/TTS stack still has
# to pass a clean install before that snapshot can become a release lock.
--extra-index-url https://download.pytorch.org/whl/cu124

# torch builds with the cu124 tag come from the index above; the pyproject
# local-cu124 extra declares the bare versions that this index satisfies.
torch==2.5.1+cu124
torchaudio==2.5.1+cu124
.[voice,vad,local-cu124]
